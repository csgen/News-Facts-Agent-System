# ─────────────────────────────────────────────────────────────────────────────
# news_facts_system — merged dependencies across the three subfolders
# scraper_preprocessing_memory + FakeNewsAgent + PredictionAgent
# Installed once into the unified Docker image.
# ─────────────────────────────────────────────────────────────────────────────

# ── Core / Config ───────────────────────────────────────────────────────────
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# ── LLM (OpenAI + optional Ollama via OpenAI-compatible endpoint) ──────────
openai>=2.0,<3.0

# ── Google Gemini (embeddings on the default cloud path) ───────────────────
google-genai>=0.8.0

# ── LangGraph / LangChain (FakeNewsAgent fact-check pipeline) ──────────────
langgraph>=0.2
langchain>=0.3
langchain-openai>=0.1
langsmith>=0.1

# ── Memory layer ────────────────────────────────────────────────────────────
neo4j>=5.0,<6.0
chromadb>=1.0.0,<2.0
rapidfuzz>=3.0,<4.0
numpy>=1.24,<3.0

# ── Observability (optional — auto-disabled if LANGFUSE_* env vars unset) ──
langfuse>=2.0,<3.0

# ── Prometheus instrumentation (used by --profile monitoring) ──────────────
prometheus_client>=0.20

# ── Live Search ─────────────────────────────────────────────────────────────
tavily-python>=0.3

# ── Scraper / Fetchers ──────────────────────────────────────────────────────
httpx>=0.27,<1.0
newspaper3k>=0.2.8,<0.3
feedparser>=6.0,<7.0
apscheduler>=3.10,<4.0
lxml_html_clean>=0.1.0

# ── NLP / Preprocessing (spaCy model downloaded in Dockerfile) ─────────────
spacy>=3.7,<4.0

# ── Cross-modal vision (FakeNewsAgent optional SigLIP / CLIP path) ─────────
transformers>=4.40
torch>=2.2
Pillow>=10.0
sentencepiece>=0.1.99

# ── Frontend (PredictionAgent Streamlit UI) ────────────────────────────────
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0

# ── Evaluation ──────────────────────────────────────────────────────────────
scikit-learn>=1.4
tqdm>=4.0

# ── Dev / Testing ───────────────────────────────────────────────────────────
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
respx>=0.21,<1.0           # HTTP-level mocking for OpenAI / Gemini / Tavily in mocked-integration tests
testcontainers>=4.0,<5.0   # spins up real Neo4j + Chroma sidecars in mocked-integration tests
ruff>=0.4,<1.0
